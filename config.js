// Copy this file to config.js and fill in your own values.
// config.js is gitignored — it will NEVER be committed or pushed.
// Never paste real keys into config.example.js or any other tracked file.
 
const CONFIG = {
  // Your Google Sheet's ID — the long string in its URL between /d/ and /edit
  // e.g. https://docs.google.com/spreadsheets/d/THIS_PART/edit
  SHEET_ID: 'YOUR_GOOGLE_SHEET_ID_HERE',
 
  SHEET_NAME: 'Sheet1',

  GROQ_API_KEY: 'YOUR_GROQ_API_KEY_HERE'
};
